import { primaryColor } from '../../constants/color';

export const style = () => ({
  container: {
    background: primaryColor,
    minHeight: '100%',
    width: '100%',
    position: 'relative',
  },
  componentContainer: {
    width: '100%',
    paddingBottom: '50px',
  },
});
